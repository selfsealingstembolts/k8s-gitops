---
apiVersion: agent.k8s.elastic.co/v1alpha1
kind: Agent
metadata:
  name: syslog-agent
  namespace: elastic
spec:
  version: 8.7.0
  kibanaRef:
    name: kibana
  fleetServerRef:
    name: fleet-server
  mode: fleet
  policyID: syslog-agent
  deployment:
    replicas: 1
    podTemplate:
      spec:
        securityContext:
          runAsUser: 0
