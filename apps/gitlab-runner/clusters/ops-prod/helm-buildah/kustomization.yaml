---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

nameSuffix: buildah

resources:
  - ../../../helm

components:
  - ../../../components/metrics

patchesStrategicMerge:
  - values.yaml
