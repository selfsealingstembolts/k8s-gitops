---
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

resources:
  - bundle.yaml

secretGenerator:
  - name: freeipa-ca
    files:
      - ca.crt
    options:
      disableNameSuffixHash: true
